
const assert = require('assert');
const fakegoose = require('../fakegoose');

describe('fakegoose', () => {
  describe('fake(...[, callback:(error, results)])', () => {
    it('does not have any tests yet', () => {});
  });

  describe('fakeOne(...[, callback:(error, result)])', () => {
    it('does not have any tests yet', () => {});
  });

  describe('seed(count[, forceAppend=false], callback:(error))', () => {
    it('does not have any tests yet', () => {});
  });
});

